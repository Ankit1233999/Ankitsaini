#include<iostream>
using namespace std;
int removeDuplicates(vector<int>&arr){
    int slow =0,
    for(int fast=1; fast < arr.size();fast++){
        if(arr)
    }
}







int main(){
   return 0;
}